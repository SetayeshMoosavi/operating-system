#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/types.h>
#include <math.h>
#include <sys/wait.h>
#include <sys/mman.h>
#include <time.h>

int main()
{
    clock_t begin = clock();
    int A[]={1,2,3,4,5,6};
    int n= sizeof(A) / sizeof(int);   
    if (n<=50);
    {
        int merge_sort(list){
             //here we have all the code of "os-priject-emtiazi3"

             return list;
        }
       
    }
    else{
        //here we are gonna have the code of "os-project-main",from line 1 to 72
        //here before line 73 we should add an if
        if(int(sizeof(B)/sizeof(int))<50){
            FINAL=merge_sort(B);
        }
        else{
            //then we are gonna have the main code from line 73  to the end
        }
    }
